services:
  postgres:
    image: postgres:18-alpine
    container_name: alt-bot-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: altbot
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U altbot -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alt-bot
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ALTBOT_DISCORD_TOKEN: ${ALTBOT_DISCORD_TOKEN}
      OWNER_ID: ${OWNER_ID}
      ALTBOT_DATABASE_URL: ${ALTBOT_DATABASE_URL}
      ALTBOT_LOG_LEVEL: info
      ALTBOT_ECONOMY_ENABLED: ${ALTBOT_ECONOMY_ENABLED}
      ALTBOT_CASINO_ENABLED: ${ALTBOT_CASINO_ENABLED}
      ALTBOT_CRYPTO_ENABLED: ${ALTBOT_CRYPTO_ENABLED}
      ALTBOT_POKER_ENABLED: ${ALTBOT_POKER_ENABLED}
      ALTBOT_ROLE_PANEL_ENABLED: ${ALTBOT_ROLE_PANEL_ENABLED}
      ALTBOT_MOD_ENABLED: ${ALTBOT_MOD_ENABLED}
    restart: unless-stopped

volumes:
  postgres_data: